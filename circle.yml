machine:
  environment:
    DATABASE_URL: postgres://ubuntu:@127.0.0.1:5432/circle_test
  python:
    version: 3.5.0
  services:
    - postgresql
  timezone:
    Europe/Moscow

test:
  override:
    - python avtozip/manage.py test
